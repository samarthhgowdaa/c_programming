#include <stdio.h>

int main(){

    // format specifiers = Special tokens that begins with a % symbol
    //                     followed by a character thta specifies the data type
    //                     and optional modifiers (width, precision, flags).
    //                     They control how data is displayed or interpreted.

    return 0;
}