#include<stdio.h>
#include<math.h>

int main (){
    
    //if output is 1, its true
    //if output is 0, its false
    
    int a=1;
    int b=2;
    int c=3;
    int d=4;

    printf("%d \n", d==d);
    printf("%d \n", a==c);
    printf("%d \n", d>c);
    printf("%d \n", d>=c);
    printf("%d \n", d<c);
    printf("%d \n", d<=a);
    printf("%d \n", d!=c);
    printf("%d \n", c!=d);
    
    //Or you can just ignore the int declaration
    //and write numbers directly under printf
    //I had created a null file, and typed the code innit
    //so I copied everything into another file,
    //and am too lazy to correct it
    //you know what they say,
    //if it works, then don't touch the code

    return 0;
}
